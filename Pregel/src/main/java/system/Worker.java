package system;

import java.util.List;
import api.Partition;

public class Worker {
	/**
	 * Adds the list of partitions to be assigned to the worker
	 * 
	 * @param partitionList
	 *            Represents a list of partition to be assigned to the worker
	 */
	public void addPartitions(List<Partition> partitionList) {

	}

}
